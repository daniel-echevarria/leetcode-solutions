/**
 * // Definition for a _Node.
 * function _Node(val, next, random) {
 *    this.val = val;
 *    this.next = next;
 *    this.random = random;
 * };
 */

var copyRandomList = function (head) {};

// Algo copyRandomList
// Given the head of a list
// Declare a variable current that gets the head
// Declare a variable newHead that gets a new Node
// newHead val gets head val
// newHead next gets a new node
