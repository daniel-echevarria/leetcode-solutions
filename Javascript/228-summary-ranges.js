var summaryRanges = function (nums) {};

console.log(summaryRanges([0, 1, 2, 4, 5, 7]));

// Algo
// declare a variable result with initial value of ''
// declare a variable previous with an initial value of nums[0] + ->
// iterate through nums from index 1
// if the current num is equal to previous + 1
